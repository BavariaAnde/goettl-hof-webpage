---
import "@fontsource/inter/400.css";
import "@fontsource/inter/500.css";
import "@fontsource/inter/600.css";
import "@fontsource/playfair-display/500.css";
import "@fontsource/playfair-display/600.css";
interface Props {
	title?: string;
	description?: string;
	preloadImage?: string;
	preloadImageType?: string;
	preloadImageSizes?: string;
	canonicalPath?: string;
	ogType?: "website" | "article";
	noIndex?: boolean;
}

const {
	title = "Göttl Hof",
	description = "Familienbetrieb aus Niederbayern mit Milchvieh, Feldern und Herz.",
	preloadImage,
	preloadImageType = "image/webp",
	preloadImageSizes = "100vw",
	canonicalPath,
	ogType = "website",
	noIndex = false
} = Astro.props as Props;
const siteUrl = "https://www.goettl-oberbubach.de";
const shareImage = `${siteUrl}/images/hof-panorama-sommer.webp`;
const canonicalUrl = new URL(canonicalPath ?? Astro.url.pathname, siteUrl).toString();
---
<!doctype html>
<html lang="de">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
		<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
		<link rel="manifest" href="/site.webmanifest" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<meta name="description" content={description} />
		<link rel="canonical" href={canonicalUrl} />
		{noIndex ? <meta name="robots" content="noindex, nofollow" /> : null}
		<meta property="og:site_name" content="Göttl Hof" />
		<meta property="og:type" content={ogType} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:url" content={canonicalUrl} />
		<meta property="og:image" content={shareImage} />
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content={shareImage} />
		{preloadImage ? (
			<link
				rel="preload"
				as="image"
				href={preloadImage}
				type={preloadImageType}
				imagesizes={preloadImageSizes}
			/>
		) : null}
		<meta name="google-site-verification" content="x5y1DFHNc_uxCcfaeaOx-FitVM5GWp-4mBQwJ6c9gkU" />

		<script
			type="application/ld+json"
			set:html={JSON.stringify({
				"@context": "https://schema.org",
				"@type": "Farm",
				"name": "Göttl Hof",
				"url": siteUrl,
				"image": shareImage,
				"address": {
					"@type": "PostalAddress",
					"streetAddress": "Oberbubach 1a",
					"postalCode": "94439",
					"addressLocality": "Roßbach",
					"addressCountry": "DE"
				},
				"contactPoint": [{
					"@type": "ContactPoint",
					"contactType": "customer service",
					"telephone": "+4985641268",
					"email": "info@goettl-oberbubach.de"
				}]
			})}
		></script>
		<!-- Dark mode: apply before render to avoid flash -->
		<script is:inline>
			(function() {
				const saved = localStorage.getItem('darkMode');
				const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
				if (saved === 'true' || (saved === null && prefersDark)) {
					document.documentElement.classList.add('dark');
				}
			})();
		</script>
	</head>
	<body>
		<a class="skip-link" href="#page-content">Zum Inhalt springen</a>
		<slot />
		<script>
			const btn = document.getElementById('darkToggle');
			btn?.addEventListener('click', () => {
				const isDark = document.documentElement.classList.toggle('dark');
				localStorage.setItem('darkMode', String(isDark));
			});
		</script>
		<script is:inline>
			(() => {
				if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
					document.querySelectorAll('[data-animate]').forEach((el) => el.classList.add('in-view'));
					return;
				}

				const observer = new IntersectionObserver((entries) => {
					entries.forEach((entry) => {
						if (entry.isIntersecting) {
							entry.target.classList.add('in-view');
							observer.unobserve(entry.target);
						}
					});
				}, { rootMargin: '0px 0px -10% 0px', threshold: 0.2 });

				document.querySelectorAll('[data-animate]').forEach((el) => observer.observe(el));
			})();
		</script>
		<script is:inline>
			(() => {
				const trackWhenReady = (name) => {
					let tries = 0;
					const tick = () => {
						if (window.umami && typeof window.umami.track === 'function') {
							window.umami.track(name);
							return;
						}
						tries += 1;
						if (tries < 20) setTimeout(tick, 300);
					};
					tick();
				};

				const observer = new IntersectionObserver((entries) => {
					entries.forEach((entry) => {
						if (entry.isIntersecting) {
							const name = entry.target.getAttribute('data-umami-view');
							if (name) trackWhenReady(name);
							observer.unobserve(entry.target);
						}
					});
				}, { rootMargin: '0px 0px -20% 0px', threshold: 0.3 });

				document.querySelectorAll('[data-umami-view]').forEach((el) => observer.observe(el));
			})();
		</script>
		<!-- Umami Analytics -->
		<script defer src="https://stats.goettl-oberbubach.de/script.js" data-website-id="bb33e9e8-6b5d-45f7-8d3d-4d5b6983e80e"></script>
	</body>
</html>

<style>
	:root {
		font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;

		/* Light mode tokens */
		--bg:          #faf7f1;
		--bg-card:     #ffffff;
		--bg-soft:     #f5f0e8;
		--bg-strong:   #ede6d6;
		--border:      #ddd3be;
		--text:        #1c1a15;
		--text-muted:  #5f5b52;
		--accent:      #3a6b35;
		--accent-dark: #2a5228;
		--accent-gold: #c79a3a;
		--accent-light:#e8f4e4;
		--shadow:      rgba(20,18,12,0.08);
	}

	html.dark {
		/* Dark mode tokens */
		--bg:          #161410;
		--bg-card:     #201e18;
		--bg-soft:     #1d1b14;
		--bg-strong:   #252219;
		--border:      #3a3626;
		--text:        #f0ead8;
		--text-muted:  #a09880;
		--accent:      #5dab55;
		--accent-dark: #4a9342;
		--accent-gold: #d4a94a;
		--accent-light:#1e2f1c;
		--shadow:      rgba(0,0,0,0.35);
	}

	html, body {
		margin: 0;
		background: var(--bg);
		color: var(--text);
		transition: background 0.3s, color 0.3s;
	}

	body {
		min-height: 100vh;
	}

	.skip-link {
		position: absolute;
		left: 1rem;
		top: 1rem;
		z-index: 2000;
		padding: 0.75rem 1rem;
		border-radius: 999px;
		background: var(--text);
		color: var(--bg-card);
		text-decoration: none;
		transform: translateY(-140%);
		transition: transform 0.2s ease;
	}

	.skip-link:focus {
		transform: translateY(0);
	}

	::selection {
		background: var(--accent);
		color: #fff;
	}

	/* Show moon in light mode, sun in dark mode */
	.toggle-icon--dark { display: none; }
	html.dark .toggle-icon--light { display: none; }
	html.dark .toggle-icon--dark  { display: inline; }

	:global(html) {
		scroll-behavior: smooth;
	}

	@media (prefers-reduced-motion: reduce) {
		:global(html) { scroll-behavior: auto; }
		:global(*), :global(*::before), :global(*::after) {
			animation-duration: 0.01ms !important;
			animation-iteration-count: 1 !important;
			transition-duration: 0.01ms !important;
		}
	}

	:global([data-animate]) {
		opacity: 0;
		transform: translateY(14px);
		transition: opacity 0.6s ease, transform 0.6s ease;
	}

	:global([data-animate].in-view) {
		opacity: 1;
		transform: translateY(0);
	}

</style>
